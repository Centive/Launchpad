[uwsgi]
deploy_env = auto
app_name = compass
project_name = project
uid = ubuntu
gid = www-data

env = COMPASS_CONFIG_FILE=/opt/piggymind/%(app_name)-config/%(deploy_env).env

app_base = /opt/piggymind/%(app_name)
env_base = /home/%(uid)/pyvenvs
env_name = %(app_name)

chdir = %(app_base)/%(project_name)
home = %(env_base)/%(env_name)
module = %(project_name).wsgi:application

master = true
processes = 5

socket = /run/uwsgi/%(app_name).sock
chown-socket = %(uid):%(gid)
chmod-socket = 660
vacuum = true
